#!/bin/sh

# use dmenu to display a list of passwords to select   
# passwords are then saved to clipboard
# if the active window is a terminal, the password will be auto pasted


LIST_OF_PASSWORDS=$(find $PASSWORD_STORE_DIR | cut -c3-)
echo $LIST_OF_PASSWORDS
